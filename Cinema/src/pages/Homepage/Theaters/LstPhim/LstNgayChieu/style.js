import { makeStyles } from "@material-ui/core"

const useStyles = makeStyles({
  lstNgayChieu: {
    paddingTop: 15,
  },
  ngayChieu: {
    fontSize: 13,
    marginBottom: 6,
    fontWeight: 500,
    color: "#fa5238",
  },
  groupTime: {
    display: 'flex',
    flexWrap: "wrap",
    gap: 8,
    marginBottom: 8,
  }
});
export default useStyles